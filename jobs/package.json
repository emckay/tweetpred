{
  "name": "tweet-pred-jobs",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:generate_predictions": "serverless deploy function -f generate_predictions",
    "invoke:generate_predictions": "serverless invoke -f generate_predictions -l --path testData.json",
    "deploy": "serverless deploy"
  },
  "author": "Eric McKay",
  "license": "UNLICENSED",
  "devDependencies": {},
  "dependencies": {
    "node-fetch": "^2.1.2"
  }
}
